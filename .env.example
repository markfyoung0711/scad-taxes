# Shared secret the Cloudflare Worker attaches as X-Butterfly-Key, and that the
# app requires before serving. Leave unset locally: with no key the gate is
# open, which is what lets `streamlit run` work without going through the proxy.
BUTTERFLY_KEY=
