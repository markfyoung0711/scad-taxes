.venv/
__pycache__/
*.pyc

# All acquired and derived data stays out of git; it belongs in GCP.
data/
data/*.log

# Secrets. BUTTERFLY_KEY lives here; it must never reach the repo.
.env
.env.*
!.env.example
